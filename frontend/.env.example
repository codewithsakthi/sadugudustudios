# Optional: Explicit backend API URL for production/staging builds
# If left empty, local environments default to http://localhost:8000/api/contacts/
# while deployed environments default to relative /api/contacts/ (or your deployed server domain).
VITE_API_URL=http://localhost:8000
